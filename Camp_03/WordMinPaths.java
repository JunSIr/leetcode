package Camp_03;
/**
* 给定两个字符串 记为strat和to，再给定一个字符串列表list,
 * list中一定包含 to;
 * list中没有重复字符串;
 * 所有字符串都是小写的;
 * 规定 start每次都只能改变一个字符，最终目标是彻底变成to
 * 但是每次变成的新字符串都必须在list中存在
 * 请返回所有最短的变换路径
 * 举例 :
 * start = "abc"
 * end = "cab"
 * list = cab acc cbc ccc cac cbb aab abb
 * 所有最短的转换路径如下
 * abc -> abb -> aab -> cab
 * abc -> abb -> cbb -> cab
 * abc -> cbc -> cac -> cab
 * abc -> cbc -> cbb -> cab
* */
public class WordMinPaths {
}
